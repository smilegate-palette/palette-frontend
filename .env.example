# 백엔드 API 서버 주소
VITE_API_BASE_URL=http://localhost:8000

# true(기본값)면 mock 데이터 사용, false면 실제 API 호출
# 백엔드 API가 준비되는 대로 false로 전환
VITE_USE_MOCK=true

# TODO: OAuth 소셜로그인(네이버/구글) 클라이언트 ID/Secret 등은
# 백엔드 담당자와 확정 후 추가 (프론트에는 client id/redirect uri 정도만 필요할 가능성 높음)
